## 开发规则

公共组件：
- 页面路由在 `src/App.tsx` 文件中注册。
- 公共组件存放在 `src/components/` 目录下
- 侧边栏菜单在 `src/components/menu.ts` 文件中注册。
- 页面存放在 `src/pages/` 目录下，文件命名遵循大驼峰。
- 通用UI组件存放在 `src/components/ui` 目录下
- 后端调用库文件位于 `src/lib/pocketbase.ts`